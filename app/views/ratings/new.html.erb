<h1> Add a rating</h1>
<%= simple_form_for [@venue, @review] do |f| %>
  <div class="form-inputs">
  <%= f.input :performer_experience, collection: 0..5 %>
  <%= f.submit "Submit rating", class: "btn btn-primary" %>
<% end %>
