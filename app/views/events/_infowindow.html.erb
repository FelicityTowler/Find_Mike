<%# <% if event.photo.attached? %>
  <%# <div class="#"> %>
      <%# <%= cl_image_tag flat.photo.key, height: 180, width: 270, crop: :fill %>
  <%# </div> %>
<%# <% end %>
<div class="mapboxgl-popup-content-stats">
  <div>
    <h2>
      <%= link_to event_path(event) do %>
        <%= event.name %>
      <% end %>
    </h2>
    <span><%= event.venue.name %></span>
  </div>
  <div>
    <i class="fas"></i><br>
    <ul>
      <li>Time: <%= @event.time.strftime("%H:%M") %></li>
      <li>Date: <%= @event.date.strftime("%d/%m/%Y") %></li>
    </ul>
  </div>
</div>