<% if @events.blank? %>
  There are no events on this date.
<% else %>
  <ul>
    <% @events.each do |event| %>
      <li>
        <%= link_to event.name,  event_path(event)%>
      </li>
    <% end %>
  </ul>
<% end %>

