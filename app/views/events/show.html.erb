<h1>
<%= @event.name %><br>
<%= @event.venue.address %><br>
<%= @event.time %><br>
<%= @event.date %><br>
<%= @event.information %><br>
<%= @event.bringer %><br>
<%= @event.available_spots %><br>
</h1>
<h2>
  <% if @event.photo.attached? %>
  <div><%= cl_image_tag @event.photo.key, height: 200, width: 8000, crop: :fill %></div>
  <% end %>
</h2>
<h3>
<% if Booking.where(["user_id = ? and event_id = ?", current_user, params[:id]]).empty? %>
<%= simple_form_for([@event, @booking]) do |f| %>
  <%= f.submit %>
<% end %>
<% else %>
<p>You are already booked on to this event</p>
<% end %>
</h3>
